#include<stdio.h>
#define w_len 5
int main(){
	int m,n;
	scanf("%d %d",&m,&n);
	char magZ[m][w_len];
	char ranS[n][w_len];
	for(i=0;i<m;i++) scanf("%s",magZ[i]);
	for(i=0;i<n;i++) scanf("%s",ranS[i]);
	
	return 0;
}